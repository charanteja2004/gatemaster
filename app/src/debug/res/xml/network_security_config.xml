<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. This file lives in src/debug, so the release APK never
  carries it and keeps the platform default: HTTPS or nothing.

  It exists so a developer can point the app at a sync server running on their
  own machine. `adb reverse tcp:8080 tcp:8080` makes the phone's localhost the
  host's localhost over the USB cable, so no LAN address, no router and no
  self-signed certificate are involved. 10.0.2.2 is the same idea for the
  emulator, which reaches its host at that address.

  Nothing else is exempted. A debug build talking to a real deployment is still
  held to HTTPS, which is what stops "it worked in debug" from being the reason
  a token went out in clear text.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
